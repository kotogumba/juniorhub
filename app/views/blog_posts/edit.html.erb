<%=  simple_form_for [@blog, @blog_post] do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.input :image, as: :file %>
  <%= f.button :submit, "Submit", class:"btn1 mt-3 mb-3"%>
<% end %>
