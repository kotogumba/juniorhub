<div class="container">
  <%=  link_to "New Blog", new_blog_path, class: "btn btn-primary" %>
  <% @blogs.each do |blog| %>
    <h1> <%=  blog.title %> </h1>
    <%= link_to "details", blog_blog_posts_path(blog), class: "btn btn-primary" %>
  <% end %>
</div>
